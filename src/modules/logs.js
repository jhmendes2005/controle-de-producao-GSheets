/* function createLog(type, desc, machine) {
    let objError = {
        type: type,
        hour: new Date(),
        machine: machine,
        description: desc,
        operador: getOperador()
    }

    setRollsArray(objError, "logs");
} */